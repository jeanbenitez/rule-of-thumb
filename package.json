{
  "name": "rule-of-thumb",
  "private": true,
  "version": "0.0.1",
  "description": "Rule of Thumb",
  "scripts": {
    "build": "stencil build",
    "build:gh-pages": "stencil build --config stencil-gh.config.ts && node scripts/fix-path-for-gh-pages.js",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "^1.12.2",
    "@stencil/router": "^1.0.1",
    "@stencil/sass": "^1.3.1",
    "@stencil/store": "^1.3.0"
  },
  "license": "MIT",
  "dependencies": {
    "local-db": "^2.0.0",
    "timeago.js": "^4.0.2"
  }
}
